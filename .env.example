# Local Core 開発用設定（Tauri経由で起動する場合は自動設定されるため不要）
ENISHI_LOCAL_TOKEN=dev-local-token
ENISHI_LOCAL_PORT=8765
# データ保存先を上書きする場合のみ指定（既定: ~/Library/Application Support/ENISHI）
# ENISHI_DATA_DIR=/path/to/data
